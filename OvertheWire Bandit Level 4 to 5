## Level 4 → 5

Task: Find the one human-readable file among several, where only one isn't binary garbage.

#Commands used:

file ./-file*
cat ./-file0X

What I learned: file identifies actual content type regardless of file name/extension — useful when a filename tells you nothing.
